# Deployment notes

This is a clean public release with a neutral first-visit demo state, global portfolio add-purchase flow, FX refresh and protocol-history polish.

Before publishing:

```bash
npm run check
```

Then create a fresh GitHub repository and deploy via Vercel.

Do not commit personal backup exports, `.git/` folders, Supabase setup files or private notes.
