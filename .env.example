# Optional only.
# The public demo works without environment variables.

# Alpha Vantage keys are normally entered inside the app and stored locally in the browser.
# Do not commit real API keys.
ALPHA_VANTAGE_API_KEY=
